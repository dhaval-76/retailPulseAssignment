import React from 'react';
import {SafeAreaView, StyleSheet, Text, View} from 'react-native';

const SearchOutlet = () => {
  return (
    <SafeAreaView>
      <Text>SearchOutlet</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});

export default SearchOutlet;
